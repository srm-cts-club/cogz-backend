- args:
    permission:
      columns:
      - id
      - name
      - college
      - fcm_id
      filter:
        id:
          $eq: X-HASURA-USER-ID
    role: user
    table: users
  type: create_select_permission
