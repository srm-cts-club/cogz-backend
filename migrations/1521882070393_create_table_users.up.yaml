- args:
    sql: CREATE TABLE "users"("id" integer NOT NULL, "name" text NOT NULL, "college"
      text NOT NULL, "fcm_id" text NOT NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: users
  type: add_existing_table_or_view
